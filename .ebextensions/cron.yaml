files:
  "/etc/cron.d/mycron":
    mode: "000644"
    owner: root
    group: root
    content: |
      * * * * * root python3.8 manage.py daily_digest
commands:
  remove_old_cron:
  command: "rm -f /etc/cron.d/mycron.bak"
